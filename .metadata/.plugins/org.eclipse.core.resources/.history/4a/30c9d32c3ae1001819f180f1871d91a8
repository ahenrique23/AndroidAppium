package appium.core;

import static appium.core.DriverFactory.getDrive;

import org.openqa.*;

public class DSL {

	public void escrever(By by, String texto) {
		getDrive().findElement(by).sendKeys();
	}
	
	public void obterTexto(By by) {
		return getDrive().findElement(by).getText();
	}
	
	
}
